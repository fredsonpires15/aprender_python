
"""
Encapsulamento(modificadores de acesso: public, protected, private) 
python não tem modificadores de acesso, mas podemos seguir as seguintes convenções

      (sem underline) = public - pode ser usado em qualquer lugar
      (um underline) = protecd - não deve ser usado fora da classe ou suas subclasses
      (dois underlines) = private - "name mangling" (desfiguração de nomes) em python
                                    só deve ser usado na classe em que foi declarado.
              
 """



